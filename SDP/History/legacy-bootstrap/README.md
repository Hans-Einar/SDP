# Historical bootstrap — not installation sources

Former root copies of payload, skills, compatibility installer and document guide. They differ from Toolkit versions and are retained as historical material; do not install them, activate their instructions or treat them as current templates. Toolkit/SDP-install.manifest.json owns the source inventory/update rules. R1 moved these files without byte changes. Git and R1's migration map preserve old addresses.
