# VER-SPS-001-002 — Installation contract remediation verification

Status: passed for independent re-review
Slice: `SPS-001`
Release context: `REL-0.2.0` (`0.2.0`, unreleased)
Candidate commit: `f81a75b96fdcc47bff4a11e9381bb62ff459a494`
Verified at: `2026-07-13T22:55:02Z`
Supersedes completion use of: `VER-SPS-001`

This verifies the remediation required by `REV-SPS-001-001`. The earlier
verification remains an exact historical record for its candidate but cannot be
used to close the Slice because independent review found five high and ten
medium issues. This record is still Slice evidence, not the final Toolkit
`0.2.0` release gate, tag or publication approval.

## Environment

- OS: Microsoft Windows `10.0.26200`
- PowerShell: Windows PowerShell `5.1.26100.8655`
- Python: `3.11.5`
- Git: `2.43.0.windows.1`
- Source: clean branch `codex/sdp-install-contract-v1`
- Base: `origin/main` at `bc110bb5fd60009ba67015cf640ad6ddbfe1b04b`
- Remediation Worker commit:
  `f81a75b96fdcc47bff4a11e9381bb62ff459a494`

## Exact Master commands and results

### Python contract and project tests

```powershell
python -m unittest discover -s Toolkit\tests -p 'test_*.py' -v
```

Result: exit `0`; `72` tests passed in `2.477s` on the Master rerun. In
addition to the original coverage, the suite now proves:

- blocked-plan exclusivity, canonical reasons and entry/reason agreement;
- contiguous action sequences, top-level/action version agreement and exact
  manifest source/generator/destination agreement;
- strict cross-platform paths, Windows aliases, case collisions, live-record
  exclusions and `.git` rejection across every contract path class;
- strict SemVer 2.0 across every public version, release-ID and tag boundary;
- review, verification, migration and resolution reverse links plus Ledger
  subject resolution;
- canonical Release/Fix `.yaml` records and record-path content identity;
- unreleased/publication nullability and release/tag/Relations identity;
- canonical governing schema/capability pairs; and
- the exact pinned `gh-sdp` closure fixture and normalized upstream tree hash.

### PowerShell installer fixtures

```powershell
.\Toolkit\tests\Install-SDP.Tests.ps1
```

Result: exit `0`; final output `Installer fixture tests passed.` The complete
suite covers original empty, repeat, project-owned, force, backup, legacy,
archive, sibling and `gh-sdp` bootstrap behavior plus the review remediation:

- strict closed-world manifest validation before target inspection;
- strict root YAML parsing, duplicate/nested-shadow rejection, RFC 3339 facts,
  unsupported schema and downgrade blocking;
- full SemVer precedence with build identity semantics;
- semantic installed-manifest equivalence without serializer churn;
- explicit deterministic target-to-target AGENTS migration whose plan matches
  apply-time I/O;
- Windows junction/reparse containment for source and target paths;
- portable path aliases, reserved devices, ADS/colon, trailing-dot/space,
  case/exclusion collisions and `.git` rejection; and
- archive bootstrap/project validation with no live `REL-0.2.0` leakage and
  truthful `sourceCommit: null` without `.git`.

The host could not create a file symlink, so that one fixture reported an
explicit skip. The equivalent Windows junction/reparse regression passed.

### Validator modes and exact consumer

```powershell
python Toolkit\scripts\validate_sdp.py
python Toolkit\scripts\validate_sdp.py --mode toolkit --base-ref origin/main
python Toolkit\scripts\validate_sdp.py --mode project --project-root Toolkit\tests\fixtures\gh-sdp-ed205c1ef193ab8a6e5cd1c50e558c3049ce6def
```

Results: all three exited `0`. Both Toolkit invocations printed
`SDP Toolkit validation passed`; project mode printed consuming-project
validation passed for the exact pinned fixture. The fixture contains 48 files,
no `.git`, and a test asserts normalized tree hash
`54f0e5854fd34e5d8bcb301f4921b956a2030e61` for upstream closure commit
`ed205c1ef193ab8a6e5cd1c50e558c3049ce6def`.

### Diff, cleanup and publication truth

```powershell
git diff --check be51ab1..f81a75b
git status --porcelain
git tag --list
gh release list --repo Hans-Einar/SDP --limit 20
```

Results: diff check passed; generated Python caches were removed; the worktree
was clean; no Git tags and no GitHub Releases existed. `SDP.manifest.yaml` and
`Releases/REL-0.2.0.yaml` still state `unreleased` with null release commit, tag
and GitHub Release identities.

## Review finding closure evidence

The remediation directly maps to all findings in `REV-SPS-001-001`:

- H1: physical source/target containment and reparse checks;
- H2: strict root YAML parser and malformed/nested-shadow gates;
- H3: schema-equivalent, closed-world PowerShell manifest preflight;
- H4: one cross-platform portable path subset and normalized exclusions;
- H5: exact offline `gh-sdp` closure validation;
- M1–M4: explicit AGENTS migration, SemVer, semantic generated facts and
  normative plan reasons;
- M5–M7: reverse traceability, canonical record extensions and publication
  identity semantics; and
- M8–M10: semantic plan validation, strict public SemVer and corrected
  schema/capability pairings.

## Limitations and next gate

- File-symlink creation was unavailable on this Windows host; junction/reparse
  behavior passed and Linux contract CI remains required on the pushed head.
- A dirty Git source checkout records its available `HEAD` as `sourceCommit`;
  that value identifies the source revision, not a cleanliness/content claim.
- A fresh Reviewer must inspect the exact integrated candidate and reproduce
  adversarial evidence. No unresolved blocking, high or medium finding may
  remain before the draft PR/CI and final Slice-closure gates.
