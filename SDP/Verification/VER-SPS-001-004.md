# VER-SPS-001-004 — Extended-path normalization verification

Status: passed for independent re-review
Slice: `SPS-001`
Release context: `REL-0.2.0` (`0.2.0`, unreleased)
Candidate commit: `1c650578109a361cf2f187da665eabdc4ac85e81`
Verified at: `2026-07-14T00:02:45Z`
Supersedes completion use of: `VER-SPS-001-003`

This verifies the single H1-R3 remediation required by
`REV-SPS-001-003`. Earlier evidence remains historical and exact. This is Slice
verification, not the Toolkit `0.2.0` release gate or publication approval.

## Environment

- Windows `10.0.26200`
- Windows PowerShell `5.1.26100.8655`
- Python `3.11.5`
- Git `2.43.0.windows.1`
- Base `origin/main`:
  `bc110bb5fd60009ba67015cf640ad6ddbfe1b04b`
- H1-R3 Worker commit:
  `1c650578109a361cf2f187da665eabdc4ac85e81`

## Exact Master evidence

### Parser, schemas and Python

PowerShell parsed the installer and fixture script, and loaded all 12 JSON
Schemas: passed.

```powershell
python -m unittest discover -s Toolkit\tests -p 'test_*.py' -v
```

Result: `73` tests passed in `2.620s`.

### Validator modes

```powershell
python Toolkit\scripts\validate_sdp.py
python Toolkit\scripts\validate_sdp.py --mode toolkit --base-ref origin/main
python Toolkit\scripts\validate_sdp.py --mode project --project-root Toolkit\tests\fixtures\gh-sdp-ed205c1ef193ab8a6e5cd1c50e558c3049ce6def
```

All three passed, including exact offline pinned `gh-sdp` project validation.

### Full PowerShell installer suite

```powershell
.\Toolkit\tests\Install-SDP.Tests.ps1
```

Result: exit `0`, `Installer fixture tests passed.` New H1-R3 coverage proves:

- distinct extended-only trailing-space and trailing-dot paths are rejected
  before prefix removal;
- ProjectRoot, BackupRoot and installer-derived source cases reject both
  PlanJson and apply with unchanged fingerprints for the supplied object,
  ordinary collision directory and target;
- drive and loopback extended UNC variants all ran and matched the dedicated
  normalization-sensitive error;
- canonical extended drive and UNC ProjectRoot/source plan and apply remain
  supported; and
- extended available 8.3 paths remain supported.

The complete prior installer matrix remained green. Loopback administrative
share was available. Only the pre-existing file-symlink fixture was skipped;
junction/reparse coverage passed.

### Diff and publication truth

`git diff --check 032a776..1c65057` passed. Generated Python caches were removed
and the worktree was clean. Local and remote tags, GitHub Releases and open SDP
pull requests were empty. `SDP.manifest.yaml` and `REL-0.2.0` remain unreleased
with null publication identities.

## H1-R3 closure claim and next gate

The installer now validates raw Windows device-prefixed input before stripping
supported `\\?\` drive/UNC prefixes. Normalization-sensitive segments, dot/
traversal forms, doubled/empty segments and unsupported device namespaces fail
closed; canonical long and 8.3 forms remain supported.

A fresh independent Reviewer must reproduce the exact wrong-project case and
confirm all previous findings remain closed. Linux contract and Windows
installer PR CI are still pending, and no blocking/high/medium finding may
remain before draft-PR and final Slice gates.
