# VER-SPS-001-003 — Residual installation-safety verification

Status: passed for independent re-review
Slice: `SPS-001`
Release context: `REL-0.2.0` (`0.2.0`, unreleased)
Candidate commit: `25fdf5cfe5a119192f512bf5322a15776e26836f`
Verified at: `2026-07-13T23:31:03Z`
Supersedes completion use of: `VER-SPS-001-002`

This verifies the bounded remediation required by `REV-SPS-001-002`. Earlier
verification records remain evidence for their exact candidates but cannot
close the Slice because later independent reviews found residual issues. This is
Slice evidence only, not the final Toolkit `0.2.0` release gate or publication
approval.

## Environment

- OS: Microsoft Windows `10.0.26200`
- PowerShell: Windows PowerShell `5.1.26100.8655`
- Python: `3.11.5`
- Git: `2.43.0.windows.1`
- Source: clean branch `codex/sdp-install-contract-v1`
- Base: `origin/main` at `bc110bb5fd60009ba67015cf640ad6ddbfe1b04b`
- Bounded Worker commit:
  `25fdf5cfe5a119192f512bf5322a15776e26836f`

## Exact Master commands and results

### Parser and schema sanity

Windows PowerShell parsed `Toolkit/scripts/Install-SDP.ps1` and
`Toolkit/tests/Install-SDP.Tests.ps1` as script blocks, then loaded all 12 JSON
Schemas. Result: exit `0`, `PowerShell parser and schema JSON sanity passed.`

### Python tests

```powershell
python -m unittest discover -s Toolkit\tests -p 'test_*.py' -v
```

Result: exit `0`; `73` tests passed in `2.635s`. New bounded coverage proves
case-insensitive destination prefix conflicts are rejected in either entry order
and all seven documented governing schema/capability pairs are parameterized and
enforced. The full prior schema, plan, release, traceability, SemVer, neutral
template and exact pinned `gh-sdp` suite remained green.

### PowerShell installer fixtures

```powershell
.\Toolkit\tests\Install-SDP.Tests.ps1
```

Result: exit `0`; final output `Installer fixture tests passed.` The suite proves:

- OS-backed Windows identity uses a 64-bit volume serial and 128-bit file ID;
- exact, parent and child source/project overlap is rejected symmetrically;
- backup/source overlap in both ancestor directions is rejected;
- local, loopback UNC, extended local, extended UNC and an available integrated
  short-name alias cannot disguise overlap;
- a physically separate same-volume local/UNC sibling remains applicable;
- PlanJson and apply are zero-mutation for every rejected overlap;
- a manifest with `AGENTS.md` and a child destination is rejected before an
  applicable plan; and
- an existing `.codex` file above canonical skill destinations blocks PlanJson
  and apply with an unchanged target fingerprint.

Loopback administrative share, extended-path and integrated short-name cases all
ran. No bounded H1/H3 capability skip occurred. The pre-existing file-symlink
fixture remained explicitly skipped because this host could not create one;
junction/reparse coverage passed.

### Validator modes and exact consumer

```powershell
python Toolkit\scripts\validate_sdp.py
python Toolkit\scripts\validate_sdp.py --mode toolkit --base-ref origin/main
python Toolkit\scripts\validate_sdp.py --mode project --project-root Toolkit\tests\fixtures\gh-sdp-ed205c1ef193ab8a6e5cd1c50e558c3049ce6def
```

All three exited `0`. Both Toolkit invocations and exact offline pinned `gh-sdp`
project validation passed.

### Diff, cleanup and publication truth

```powershell
git diff --check d7da022..25fdf5c
git status --porcelain
git tag --list
git ls-remote --tags origin
gh release list --repo Hans-Einar/SDP --limit 20
```

Diff check passed; generated caches were removed; the worktree was clean; local
and remote tag lists and GitHub Releases were empty. `SDP.manifest.yaml` and
`Releases/REL-0.2.0.yaml` remain `unreleased` with null release commit/
preparation commit, tag and GitHub Release identities.

## Residual review finding evidence

- H1-R2: stable filesystem identity, symmetric ancestry, fail-closed identity
  acquisition and the local/UNC/extended/short-name matrix.
- H3-M8-R2: manifest prefix and existing non-directory ancestor preflight before
  planning, immediate pre-mutation recheck and zero-mutation regressions.
- M10-R2: one seven-pair Python mapping and parameterized regression for every
  governed schema.
- Dirty-checkout provenance: public docs now say non-null `sourceCommit` is an
  available `HEAD` baseline, not proof that installed bytes equal the commit.

## Limitations and next gate

- Windows `FileIdInfo` fails closed when unavailable, including unsupported
  older or network filesystems. Different share roots on one device are rejected
  when ancestry above the share cannot be proven. These boundaries are public.
- File-symlink creation was unavailable locally; junction/reparse behavior
  passed. Non-Windows bind-mount evidence and Linux contract execution remain
  pending from draft-PR CI where applicable.
- A fresh Reviewer must inspect the exact integrated candidate and reproduce the
  bounded counterexamples. No unresolved blocking, high or medium finding may
  remain before draft-PR and final Slice gates.
