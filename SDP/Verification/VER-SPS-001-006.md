# VER-SPS-001-006 - Downstream-conformance revision verification

Status: passed for Slice revision completion
Slice: `SPS-001`
Revision: `1`
Release context: `REL-0.2.0` (`0.2.0`, unreleased)
Candidate commit: `a37a8fa298fa3ace2ec5826bc3a30f26f90a64ac`
Verified at: `2026-07-17T15:24:53Z`
Supersedes completion use of: `VER-SPS-001-005`
Trigger review: `REV-SPS-001-006`
Independent implementation review: `REV-SPS-001-007`
Draft pull request: `https://github.com/Hans-Einar/SDP/pull/4`

This record verifies the bounded revision that closes the three downstream
Medium findings: canonical plan ordering, complete deterministic AGENTS
collision/idempotency/race semantics and a shared language-neutral conformance
package. It is not a Toolkit `0.2.0` release gate or publication authorization.

## Verified contract

The required machine-readable ordering policy is
`migration-first-manifest-order-v1` in both the installation manifest and every
plan. Blocked plans contain one sequence-1 action. Migrations form a
deterministic prefix, ordinary actions follow selected manifest array order,
backup/mutation pairs are adjacent and identity-equal, and final sequence values
are contiguous.

The AGENTS preservation destination uses lowercase SHA-256 of exact source
bytes. Absent, identical-file, different-file, unsupported-object and
plan/apply-race cases have one public outcome each. Stable fatal classes cross
the PowerShell process boundary, preflight failures are mutation-free and apply
never replaces project-owned preservation content.

`Toolkit/conformance/install-v1/` provides 17 portable scenarios and committed
expected outcomes: 11 applicable plans, 3 blocked plans and 3 fatal outcomes.
The scenario model, before states, options, assertions and expected files are
language-neutral and require no `.git`, absolute path, timestamp or PowerShell
interpretation.

## Exact local evidence

Worker, Master and fresh Reviewer evidence collectively passed:

- `python -m pip install -r Toolkit/tests/requirements.txt`;
- `python Toolkit/scripts/validate_sdp.py --mode toolkit --base-ref origin/main`;
- `python -m unittest discover -s Toolkit/tests -p "test_*.py" -v` - 80 tests,
  `OK`;
- `python Toolkit/conformance/install-v1/run_conformance.py --validate-only` -
  17 scenarios;
- exact project validation against
  `Toolkit/tests/fixtures/gh-sdp-ed205c1ef193ab8a6e5cd1c50e558c3049ce6def`;
- the full `Toolkit/tests/Install-SDP.Tests.ps1` suite under Windows PowerShell
  `5.1.26100.8875` and PowerShell `7.6.3`, each with the final success marker;
- every conformance scenario through the reference harness under both
  PowerShell hosts;
- both PowerShell parser checks, all schema/JSON/YAML/NDJSON checks and
  `git diff --check`; and
- archive/no-`.git`, exact pinned consumer, deterministic repeat, race,
  zero-mutation, path-safety and publication-state cases.

Master verification independently repeated the 80-test suite, both validators,
both full installer suites, both 17-scenario reference runs, portable package
validation, parser checks and branch diff checks. Generated Python caches were
removed before push.

## Adversarial review evidence

`REV-SPS-001-007` independently:

- reordered manifest entries and observed exact manifest-order planning;
- split a backup/mutation pair and observed semantic rejection;
- exercised absent, identical, differing, directory and junction AGENTS hash
  targets;
- proved exact-byte hashing, deterministic repeats and zero mutation on fatal
  preflight outcomes;
- changed a committed expected plan and observed reference-comparison failure
  without candidate regeneration; and
- validated all 17 fixture references with an independent JSON/JSON-Schema
  checker that imported neither the installer nor the reference harness.

The review approved the exact candidate with zero Blocking, High, Medium or Low
findings.

## Exact GitHub Actions evidence

Workflow run:
`https://github.com/Hans-Einar/SDP/actions/runs/29590324256`

The pull-request run completed successfully on exact head
`a37a8fa298fa3ace2ec5826bc3a30f26f90a64ac`.

- Linux `contracts` job:
  `https://github.com/Hans-Einar/SDP/actions/runs/29590324256/job/87917499632`
  - result: passed in `14s`;
  - Toolkit validation passed;
  - all 80 tests passed in `3.975s`; and
  - portable package validation passed for 17 scenarios.
- Windows `installer` job:
  `https://github.com/Hans-Einar/SDP/actions/runs/29590324256/job/87917499648`
  - result: passed in `8m44s`;
  - the full hosted fixture suite ended with
    `Installer fixture tests passed.`;
  - no file-symlink skip marker appeared, so the previously failing hosted case
    executed; and
  - all 17 named reference scenarios passed under `pwsh`.

Run `29585982002` on earlier merge head `6b326543b3712562338e26dfe978b04440411e84`
remains truthful failed evidence for the missing hosted file-symlink failure
class. Commit `a37a8fa298fa3ace2ec5826bc3a30f26f90a64ac` is the bounded reviewed
remediation.

## Publication truth and gate result

Toolkit version remains `0.2.0` with state `unreleased`. Release commit, Git tag
and GitHub Release identities remain null. Local tags, remote tags and GitHub
Releases are empty. PR #4 remains open and draft. No merge, release, tag or
publication occurred.

`SPS-001` revision 1, `SPI-001` and `Sprint-001` may close. The repository is
ready only for a new bounded `gh-sdp` downstream acceptance pass. PR #4 must not
be marked ready until that group returns `ACCEPTED` or
`ACCEPTED_WITH_LOW_FINDINGS` with no merge-blocking item.
