# SDUI — planning evidence, 2026-09-20

Documentation/design-model checking, not new parser/runtime/GUI verification. Work started
on sdp-vNow 9ad4324 with the preceding session's documentation changes already present;
they were continued/corrected according to owner input.

## Changes

- Removed backward-compatible SDUI/BoxUI requirements from active plans/handoffs.
- Wrote frame/layout proposal and target architecture; implementation was planned.
- Assigned P0–P6 owners, dependencies, milestones and acceptance criteria.
- Expressed the proposed architecture in implemented SDL design-core 0.1.
- Python parser replacement remained pending; P1-M4/P5-M4/P6-M2 specified cleanup,
  without claiming it already happened.

## Checks performed

```sh
python3 experiments/design_core/design_core.py check SDUI/design/architecture.design
git diff --check
```

After canonical formatting, SDL returned valid: true without diagnostics. It checked
syntax, ownership, references/types and canonical structure, not layout/runtime implementation.
Relative links were checked against files. Git/worktrees were inspected; Mermaid master had
no src/boxui and needed no removal. No renderer/XFMD source or worktree was altered/deleted.
No new parser regressions, GUI/PDF runs or installation occurred. September 19 results
remain historical baseline, not new-profile evidence. No commit/push.

## Addendum: relative dimensions and aspect ratio

Owner clarification entered layout/requirements/architecture/plan/XFMD handoff: no pixel
width/height, ancestor-relative scale, x:y ratio, one driving axis with ratio, canonical
mini arrows. Concept1 weights/FixedAspectViewport were inspected in code/CSS and recorded
in the [study](../docs/concept1-relative-layout-study.md).

Links existed, fences balanced, new examples had no width/height assignments or px literals.
Ratio examples were recomputed with exact fractions. These are documentation/arithmetic
checks, not layout-engine tests. git diff --check passed. Reflow versus whole-surface
transformation was still open at this date.
