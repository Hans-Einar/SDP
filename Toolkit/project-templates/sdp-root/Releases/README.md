# Releases

Store one project release record per target version using ID
`REL-MAJOR.MINOR.PATCH`. Records link included work, release notes, verification,
review, migration, the release-preparation commit, annotated tag and GitHub
Release.

Publication fields remain null until the corresponding objects exist. Released
records are immutable except through explicit correction or yank events.
